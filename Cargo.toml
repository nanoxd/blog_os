[package]
name = "blog_os"
version = "0.1.0"
authors = ["Fernando Paredes <nano@fdp.io>"]

[dependencies]
rlibc = "1.0.0"
volatile = "0.2.3"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
